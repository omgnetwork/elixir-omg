# This config will be concatenated to test.config on jenkins

# This setting is needed because we are running test from root on Jenkins
# and erlexec's is hell-bent on doing it safely.
config :erlexec,
  user: 'geth',
  limit_users: ['geth']
